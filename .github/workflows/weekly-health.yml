name: Weekly Health

on:
  schedule:
    - cron: "0 16 * * 1"
  workflow_dispatch:

jobs:
  health:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run primary checks
        shell: pwsh
        run: ./scripts/primary-checks.ps1
