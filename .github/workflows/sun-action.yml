name: Sun Action

on:
  schedule:
    - cron: "0 17 * * 0"
  workflow_dispatch:

jobs:
  sun_action:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Run primary checks
        shell: pwsh
        run: ./scripts/primary-checks.ps1
