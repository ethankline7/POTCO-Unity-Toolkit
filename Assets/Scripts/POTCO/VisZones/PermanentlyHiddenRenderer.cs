using UnityEngine;

namespace POTCO.VisZones
{
    /// <summary>
    /// Marker component to indicate a renderer should NEVER be shown by VisZone system
    /// Added by HideLevelGeometry to pir_m_prp_lev_* objects
    /// </summary>
    public class PermanentlyHiddenRenderer : MonoBehaviour
    {
        // This is just a marker component - no logic needed
    }
}
